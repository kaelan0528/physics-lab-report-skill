---
name: physics-lab-report
description: 根据大学物理实验教材或指导书、原始数据、可选旧报告模板和目标 Word，完成材料审计、数据处理、不确定度、绘图和 Word 成品输出。用户要求与本次教材优先，适用于跨实验报告制作及局部修订。
---

# 大学物理实验报告

本 Skill 内置从用户资料生成报告的工作流，不内置任何实验答案。仅处理用户授权的阶段与文档范围。

## 依据与执行边界

优先级：用户当前明确要求 → 本次教材/指导书 → 本次原始记录 → 往期报告模板 → 通用大学物理实验知识。该顺序用于决定方法与格式，不允许据此改造观测值。教材与记录冲突时暂停相关处理并询问。

用户要求“一步一步来”或指定某阶段时，完成该阶段即停止，等待下一条指令；完整报告请求则在无冲突时依序继续。数据模糊、仪器参数不清、同值异义、不确定度口径不清、公式无法确定或教师要求显著影响结果时，说明具体冲突并询问，暂停依赖该答案的工作。不要把既有授权变成重复审批。

往期报告只参考结构、格式、数据处理展开方式、不确定度写法和结果分析风格，不能复制数据或物理结论。教师要求作为本次课程依据记录，不以通用知识静默覆盖教材。

## Stage 1 材料审计

识别本次教材、原始记录、旧模板、目标 Word 的角色；同一文件可有多个角色。列出文件、用途、可读性、教材页码及目标文档已有与空缺章节。区分实际渲染页数与文档缓存页数。

整理原理、公式、常量、仪器参数、处理要求、思考题原题及方法出处。判定是否要求逐点计算、平均、拟合、坏值检验与绘图，不从旧模板套用物理方法。此阶段不正式计算、不修改目标文档。

## Stage 2 原始数据冻结

读取 [数据 Schema](templates/data-freeze-schema.json)，生成符合其结构的 `data_freeze.json`。OCR 只生成候选值；逐项视觉核对、检查表内关系、在允许范围以公式反算交叉核验，并核对单位和数量级。算术一致不能证明模糊字迹正确；同源照片副本也不算两次独立测量。

分开记录直接测量、现场手算派生值、仪器参数、修正量和后续计算结果。以十进制字符串保存原始精度和尾随零，单位单列，不能把现场平均值当独立测量。来源使用相对资料标识与页码/表格位置，不写本机绝对路径。记录人工确认、核对证据、歧义、舍入顺序；无法辨认必须询问，不能靠算术猜填。

冻结前确认全部计算输入已核准、无待决问题；版本和来源摘要可追溯。后续所有数值计算只读取冻结文件，禁止再次用图片 OCR 输入计算。变更需形成新版本、记录理由并使依赖结果失效，不能静默覆盖。最终结果放入独立计算产物，不回填并污染原始冻结版本。

## Stage 3 计算与独立验算

在冻结数据上按教材确定主方法，区分“教材明确规定”与“常规方法建议”；关键方法无依据且影响结果时先询问。不在缺失参数时计算完整结果。

使用可执行计算工具，保留足够中间精度，最终展示才舍入。转换单位时保留换算关系。以不同公式、独立实现或手工推导至少验算一次关键结果，不能把同一函数运行两次当独立验算；检查量纲、数量级与舍入影响。保存输入版本、方法出处、未舍入值和展示值。

## Stage 4 坏值判断

仅当教材、教师或旧模板要求时执行；旧模板要求仍须与当前物理量及统计条件相容，否则询问。未要求则记录“不适用”，不得默认使用 3σ。

按明确要求选 3σ、Grubbs 或其他判据。3σ 展示平均值 → 残差 → 贝塞尔样本标准差 → 3s → 每项比较 → 结论；Grubbs 需给定显著性水平与适用条件。不对不同条件下的非重复数据机械套用重复测量判据。

发现异常先核对记录，不能自动删除；只有明确依据和授权才排除，记录理由，保留原数据并重新验算受影响结果。

## Stage 5 不确定度与修正量

分别识别零点读数、系统修正量、仪器误差限、A 类、B 类与合成标准不确定度。数值相等也不代表相同物理量。

按教材符号处理修正，如读数偏置为 z，则修正后的量为测量值减 z；实际关系须来自本次资料。修正量自身不是随机不确定度，不能直接重复加入；若其估计有独立不确定度，按有来源的模型传播。

A 类基于适用的重复测量。独立同分布重复观测下可用样本标准差 s 与均值标准不确定度 u_A=s/√n；非重复、相关或回归数据按相应模型，不一律套用。

B 类须有仪器误差限、最大允许误差、分辨率、校准或教材明确参数，并记录来源。只有教材或课程明确均匀分布，才用 u_B=Δinstrument/√3；分辨率不能直接当误差半宽。缺少参数或分布时询问，不编造；仅能给 A 类时明确其适用范围。

不相关分量可用 u=√(u_A²+u_B²)；相关分量须计协方差。派生量优先按教材传播式；适用条件需说明。明确 Δ、标准不确定度、扩展不确定度和覆盖因子之间的关系，不隐含换算或假定覆盖水平。前文定义的 Δ 必须与后文传播一致。

## Stage 6 绘图与辅助分析

仅在实验要求或用户需要时绘图。中文图题，轴变量和单位清楚，实测点与拟合线可区分，方程系数与坐标单位对应。拟合模型必须有物理依据，不能为显得复杂强加拟合。R² 只作辅助，不证明准确度；拟合斜率不能擅自替代教材规定的逐点计算平均结果。检查残差、拟合条件及截距的物理意义（适用时）。

## Stage 7 正文撰写

以已核准结果写连贯自然段：数据规律、结果及不确定度、具体误差来源、方法评价。不能把重复性或高 R² 当绝对准确，不能猜材料或虚构标准值。不重复整表或详细计算；保持模板风格。

逐题回答教材原题，按结论、原理/公式、物理解释展开。拓展仅在用户或模板要求时添加，贴近本次实验。正文不放 Step、Codex、JSON、workspace 等工作流痕迹；这些记录留在单独 QA 说明中。

## Stage 8 Word 写入与公式

修改前备份，限定修改章节；保留已有标题样式、字体字号、层级、原始图片和非授权内容。正文服从目标模板与用户字体要求，不套通用美化方案，不用汇报式 bullet/listicle，不把每句拆一段。

正式独立公式使用 OMML Professional：真正分式、根号、上下标、求和结构，通常独立居中；不要用 LaTeX 源码、sqrt(...)、a/b 文本或截图替代。正文变量尽量 inline Office Math，维持中文句子连贯。数学字体使用正常数学字体，不强制正文中文字体。缺少可验证的公式工具时说明能力缺口，不伪称完成。

旧转换器兼容副本仅在用户明确请求时制作，保留专业公式原稿，先说明会改变公式表示；不能默认降级原稿，也不能仅因含 OMML 就断言文档损坏。

### 列表与分页控制

区分真正列表（w:numPr、List Paragraph、numbering 的 bullet 类型）与编辑界面非打印分页标记。keepNext/keepLines 可能触发标记，pageBreakBefore 等也可能相关；不要把所有黑点一概视为 bullet，先检查实际属性和样式继承。

普通正文默认 keepNext、keepLines 关闭；若样式继承为开，仅删除直接属性不够，需显式关闭。真实标题仅保留必要 keepNext。移除不需要的列表引用时解析编号类型，保留正常文本编号和必要自动标题编号；改变列表样式需保留有效字体、缩进、间距。

表格行优先用 cantSplit 防止行内拆分，不给每个单元格加 keepNext 链。cantSplit 不能保证整个表同页；小表如需整体同页，合理调整前置图尺寸或将表整体分页，并真实渲染验证。避免孤立标题、过大空白，不为减少页数挤压公式或字号。

## Stage 9 最终 QA

使用 [最终检查清单](templates/final-qa-checklist.md)。核对数据、公式、单位、不确定度、图表、正文及修改范围；比较受保护内容，检查 DOCX ZIP/XML、关系目标和图片能否读取，重新打开文档。

尽可能用本机 Word 只读导出临时 PDF，或可用的可靠渲染器转换，再逐页查看图像。核查实际页数、标题孤立、表格、公式、图片、字体、空白与末页。渲染超时/失败应记录原因，不能用缓存页数代替，不安装新软件或上传私密资料到外部转换服务以绕过限制。

无法渲染必须原样报告：“只完成结构检查，未完成真实视觉检查。” 不能把 XML 通过写成视觉 PASS。最终交付文档路径、已完成检查、限制和待决问题；交付不等于自动提交课程网站或公开发布。

## 隐私与公开边界

用户资料仅用于本次任务。公开仓库不得收录可识别个人的信息、身份编号、教学人员私人信息、真实记录、私有教材、内部产物或本机路径；示例必须完全虚构。检查正文、文件名、元数据和版本历史。忽略规则不是保密保证，不能自动上传用户文档。
